server:
  port: 8061

spring:
  application:
    name: discovery-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
		server:
		    enable-self-preservation: false